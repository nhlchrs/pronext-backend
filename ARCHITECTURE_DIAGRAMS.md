# 🎯 Architecture Diagrams - Phase 1

## 1️⃣ Session Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│                    USER LOGIN FLOW                       │
└─────────────────────────────────────────────────────────┘

  ┌──────────────┐
  │ User Login   │
  │ (email/pwd)  │
  └──────┬───────┘
         │
         ▼
  ┌──────────────────────────────┐
  │ Verify Credentials           │
  │ Check if suspended           │
  │ Check daily limit            │
  └──────┬───────────────────────┘
         │
         ▼
  ┌──────────────────────────────┐
  │ Enforce Single Session       │
  │ Deactivate old sessions      │ ◄─── KEY: Previous tokens invalid!
  │ UPDATE: isActive = false     │
  └──────┬───────────────────────┘
         │
         ▼
  ┌──────────────────────────────┐
  │ Generate JWT Token           │
  │ Payload: _id, name, email    │
  │ Expiry: 1 day                │
  └──────┬───────────────────────┘
         │
         ▼
  ┌──────────────────────────────┐
  │ Create Session Record        │
  │ Store: token, user, IP,      │
  │ device, timestamps, expiry   │
  └──────┬───────────────────────┘
         │
         ▼
  ┌──────────────────────────────┐
  │ Return to User               │
  │ { token, user, message }     │
  └──────────────────────────────┘
```

---

## 2️⃣ Database Schema Relationships

```
┌─────────────────────────────────────┐
│ USERS Collection                    │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ fname, lname, email, phone          │
│ password (hashed)                   │
│ role: Admin|Finance|Educator        │
│ isSuspended, dailyLoginCount        │
│ createdAt, updatedAt                │
└────────────┬────────────────────────┘
             │
             │ 1 to Many
             │ (one user can have many sessions)
             │
             ▼
┌─────────────────────────────────────┐
│ SESSIONS Collection                 │
├─────────────────────────────────────┤
│ _id: ObjectId                       │
│ user: ObjectId (FK to Users)        │
│ token: String (JWT)                 │
│ ipAddress: String                   │
│ userAgent: String                   │
│ isActive: Boolean                   │
│ loginTime, lastActivityTime         │
│ expiresAt (TTL Index)               │
│ createdAt, updatedAt                │
└─────────────────────────────────────┘
```

---

## 3️⃣ Request Processing Flow

```
┌─────────────────────────────────────────────────────────┐
│              INCOMING API REQUEST                        │
└─────────────────────────────────────────────────────────┘

       ┌──────────────────────────────────┐
       │ 1. Extract Authorization Header  │
       │    Authorization: TOKEN          │
       └──────────────────┬───────────────┘
                          │
                          ▼
       ┌──────────────────────────────────┐
       │ 2. JWT Verification              │
       │    Decode & verify JWT signature │
       │    Extract user info             │
       └──────────────────┬───────────────┘
                          │
                          ▼
       ┌──────────────────────────────────┐
       │ 3. Session Validation            │
       │    (sessionMiddleware)           │
       │    - Find session in DB          │
       │    - Check isActive = true       │
       │    - Check expiresAt > now()     │
       └──────────────────┬───────────────┘
                          │
                    ┌─────┴─────┐
                    │           │
                   ✅           ❌
            (Session Valid) (Session Invalid)
                    │           │
                    ▼           ▼
       ┌────────────────────┐ ┌─────────────────┐
       │ 4. Update Activity │ │ Return Error    │
       │ lastActivityTime   │ │ 401 Unauthorized│
       │ = now()            │ └─────────────────┘
       └──────────┬─────────┘
                  │
                  ▼
       ┌────────────────────┐
       │ 5. Process Request │
       │ Call route handler │
       └──────────┬─────────┘
                  │
                  ▼
       ┌────────────────────┐
       │ 6. Return Response │
       │ { status, data }   │
       └────────────────────┘
```

---

## 4️⃣ Multi-Device Scenario

```
Timeline:

T1: User logs in from PHONE
    ├─ Deactivate previous sessions (none)
    ├─ Generate Token A
    ├─ Create Session A (isActive: true)
    └─ Return Token A to user
       Session: { token: A, isActive: true }

T2: Same user logs in from LAPTOP
    ├─ Deactivate previous sessions (Session A!)
    │  UPDATE sessions SET isActive=false WHERE user=X, isActive=true
    ├─ Generate Token B
    ├─ Create Session B (isActive: true)
    └─ Return Token B to user
       Session A: { token: A, isActive: FALSE }  ❌
       Session B: { token: B, isActive: true }   ✅

T3: PHONE tries to access API with Token A
    ├─ Extract Token A from Authorization header
    ├─ Verify JWT (OK)
    ├─ Find Session with Token A
    ├─ Check: isActive = true? NO ❌
    └─ Return: 401 Session expired or invalid

T4: LAPTOP uses Token B (works normally)
    ├─ Extract Token B from Authorization header
    ├─ Verify JWT (OK)
    ├─ Find Session with Token B
    ├─ Check: isActive = true? YES ✅
    ├─ Update: lastActivityTime = now()
    └─ Process request normally
```

---

## 5️⃣ Session Lifecycle

```
BIRTH (Login)
    ▼
  ┌─────────────────────────────────────┐
  │ Session Created                     │
  │ isActive: true                      │
  │ expiresAt: now() + 24 hours         │
  └──────────────────┬──────────────────┘
                     │
                     ├─ Each API call ─────┐
                     │ lastActivityTime ◄──┘
                     │ gets updated
                     │
       ┌─────────────┴──────────────┐
       │                            │
       ▼                            ▼
  ┌──────────────────┐      ┌─────────────────────┐
  │ DEATH: Logout    │      │ DEATH: Expiration   │
  │ User clicks      │      │ 24 hours pass       │
  │ logout button    │      │ OR Browser timeout  │
  │                  │      │                     │
  │ isActive: false  │      │ MongoDB TTL removes │
  │ (manual)         │      │ (automatic)         │
  └──────────────────┘      └─────────────────────┘
       │                            │
       └────────────┬───────────────┘
                    │
                    ▼
          ┌──────────────────────┐
          │ Session Inactive     │
          │ Cannot be used       │
          │ User must login again│
          └──────────────────────┘
```

---

## 6️⃣ Admin Control Flow

```
┌──────────────────────────────────────────────┐
│            ADMIN DASHBOARD                   │
└──────────────────────────────────────────────┘
              │
              │ GET /api/sessions
              │
    ┌─────────▼──────────┐
    │ View All Sessions  │
    │ - User names       │
    │ - IP addresses     │
    │ - Last activity    │
    │ - Expiry times     │
    └─────────┬──────────┘
              │
    ┌─────────▼──────────────┐
    │ See suspicious user?   │
    │ DELETE /api/session/:ID│
    └─────────┬──────────────┘
              │
              ▼
    ┌────────────────────────┐
    │ Session terminated     │
    │ isActive: false        │
    │ User logged out        │
    │ User must login again  │
    └────────────────────────┘
```

---

## 7️⃣ Error Handling Chain

```
Request comes in
    │
    ├─ No Authorization header?
    │  └─► 401 JWT token missing
    │
    ├─ Invalid JWT signature?
    │  └─► 401 Unauthorized Access
    │
    ├─ Session not found?
    │  └─► 401 Session expired or invalid
    │
    ├─ isActive = false?
    │  └─► 401 Session expired or invalid
    │
    ├─ expiresAt < now()?
    │  └─► 401 Session expired or invalid
    │
    ├─ User is not Admin (for admin route)?
    │  └─► 403 Admin access required
    │
    └─ All checks pass?
       └─► Process request normally ✅
```

---

## 8️⃣ Security Layers

```
┌─────────────────────────────────────────────┐
│         SECURITY ARCHITECTURE               │
└─────────────────────────────────────────────┘

Layer 1: JWT Signature
    ├─ Secret key: PROCESS.ENV.JWT_SECRET
    ├─ Algorithm: HS256
    └─ Can't forge without secret

Layer 2: Token in Database
    ├─ Token stored in sessions collection
    ├─ Cross-verify JWT with DB
    └─ Prevents token replay attacks

Layer 3: Session Status
    ├─ isActive flag
    ├─ Previous logins auto-deactivate
    └─ Only 1 active session per user

Layer 4: Expiration
    ├─ Token expires: 24 hours
    ├─ Session expires: 24 hours
    ├─ MongoDB auto-deletes: TTL index
    └─ Extra: Activity timeout (optional)

Layer 5: Activity Tracking
    ├─ IP address captured
    ├─ Device info stored
    ├─ Last activity time tracked
    └─ Admin can review suspicious activity

Layer 6: Admin Controls
    ├─ Force logout capability
    ├─ Session monitoring
    ├─ User account management
    └─ Activity audit logs
```

---

## 9️⃣ Data Flow on Protected Route

```
Frontend sends:
┌──────────────────────────┐
│ GET /api/data            │
│ Authorization: TOKEN_XYZ │
└─────────────┬────────────┘
              │
              ▼
Backend processes:
┌──────────────────────────────────────┐
│ 1. Parse JWT Token                   │
│    decode(TOKEN_XYZ, JWT_SECRET)     │
│    ✅ Valid ► Extract _id, email    │
│    ❌ Invalid ► Return 401            │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ 2. Query Session Collection          │
│    db.sessions.findOne({             │
│      token: TOKEN_XYZ,               │
│      isActive: true,                 │
│      expiresAt: {$gt: now()}         │
│    })                                │
│    ✅ Found ► Validate               │
│    ❌ Not found ► Return 401          │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ 3. Update Activity                   │
│    session.lastActivityTime = now()  │
│    session.save()                    │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ 4. Execute Route Handler             │
│    with authenticated user info      │
│    (req.user._id, req.user.email)   │
└──────────────────┬───────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ 5. Return Response to Frontend       │
│    { status: 1, data: {...} }        │
└──────────────────────────────────────┘
```

---

## 🔟 State Machine: Session States

```
                    ┌─────────────┐
                    │   CREATED   │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   ACTIVE    │
         ┌────────► │ (isActive)  │ ◄────────┐
         │          └──────┬──────┘          │
         │                 │                  │
    Used │          Can use │              Each API
  token A │      with token │              updates
         │          B       │            lastActivityTime
         │                 │
         │     ┌───────────┴───────────┐
         │     │                       │
         ▼     ▼                       ▼
    ┌─────────────────┐        ┌──────────────────┐
    │ INACTIVE        │        │ AUTO-EXPIRED     │
    │ (Logout/New     │        │ (24 hours pass)  │
    │  Session)       │        │ TTL index        │
    │                 │        │ removes it       │
    │ isActive: FALSE │        │                  │
    │                 │        │ (no DB record)   │
    └────────┬────────┘        └────────┬─────────┘
             │                          │
             └──────────┬───────────────┘
                        │
                        ▼
                 ┌─────────────┐
                 │ DELETED     │
                 │ Must login  │
                 │ again       │
                 └─────────────┘
```

---

These diagrams visualize the complete session management architecture implemented in Phase 1!
